"use client";

import { Panel } from "./panel";

export function LayersPanel() {
  return (
    <Panel title="Layers">
      <p className="text-xs text-zinc-500 italic">No layers yet.</p>
    </Panel>
  );
}
